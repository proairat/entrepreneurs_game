enum EntityState {
  Active = "active",
  Default = "default",
  Close = "close",
}

enum EntityType {
  Topics = "topics",
  Tests = "tests",
  Modules = "modules",
}

export { EntityState, EntityType };
