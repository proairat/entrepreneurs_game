enum CourseState {
  Active = "active",
  Default = "default",
  Close = "close",
}

enum TopicState {
  Active = "active",
  Default = "default",
  Test = "test",
}

export { CourseState, TopicState };
